{
  "name": "drewify",
  "version": "0.0.1",
  "description": "Retrieve some character sheets (sourced in markdown) from Drew's repo and render them.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Christopher Bennage",
  "license": "MIT",
  "dependencies": {
    "github": "^0.2.4",
    "marked": "^0.3.3"
  }
}
